function HomePage() {
    return (
        <div style={{ padding: '1rem'}}>
            <h2 style={{ color: '#0f172a' }}>Welcome to the Student Portal Home Dashboard!</h2>
            <p>Select a tab above to explore your workspace dashboard.</p>
        </div>
    );
}

export default HomePage; // Make sure this line is explicitly at the bottom
